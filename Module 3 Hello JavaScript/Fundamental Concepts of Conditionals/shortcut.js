const isLeader = true;

// (isLeader === true)
if (isLeader) {
    console.log("Give him the chair");
}
else {
    console.log("You can not enter");
}

// (isLeader === flase)
if (!isLeader) {
    console.log("Give him the chair");
}
else {
    console.log("You can not enter");
}