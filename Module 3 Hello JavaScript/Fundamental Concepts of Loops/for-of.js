const numbers = [12, 343, 34, 4354, 535];
console.log(numbers); //it will print all the numbers

for (const num of numbers) {
    console.log(num);
}

const fruits = ['orange', 'apple', 'mango', 'strawberry', 'jackfruit'];

// for(){

// }

for (const fruit of fruits) {
    console.log(fruit);
}